targets:
  $default:
    sources:
      - test/**
      - lib/**
      - web/**
      # Note that it is important to include these in the default target.
      - pubspec.*
      - $package$
    builders:
      json_serializable:json_serializable:
        generate_for:
          - lib/**/*.dart